source 'https://rubygems.org'

# Matches the Jekyll version GitHub Pages actually builds with.
gem 'github-pages', group: :jekyll_plugins

group :jekyll_plugins do
  gem 'jekyll-sitemap'
  gem 'jekyll-feed'
end
