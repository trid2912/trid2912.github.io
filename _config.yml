#
# Site configuration
#

# Name of your site (displayed in the header)
name: Duc-Tri Tran

# Short bio or description. Left empty on purpose: the name alone is
# what should show in the browser tab and in search results.
description:

# URL of your avatar or profile pic (circular, shown in the header)
avatar: /images/tran-duc-tri.jpg

#
# Flags below are optional
#

# Includes an icon in the footer for each username you enter
footer-links:
  dribbble:
  email: tri.td291202@gmail.com
  facebook:
  flickr:
  github: trid2912
  instagram:
  linkedin: tri-tran-192254200
  pinterest:
  rss:
  twitter:
  stackoverflow:
  youtube:
  googleplus:

# Enter your Disqus shortname (not your username) to enable commenting on posts
disqus:

# Enter your Google Analytics web tracking code (e.g. UA-2110908-2) to activate tracking
google_analytics:

# Your website URL - used for sitemap.xml and the RSS feed
url: https://trid2912.github.io

# Leave empty for a User repository (trid2912.github.io)
baseurl: ""

#
# !! You don't need to change any of the configuration flags below !!
#

permalink: /:title/

# The release of Jekyll Now that you're using
version: v1.2.0

# Jekyll 3 now only supports Kramdown for Markdown
kramdown:
  input: GFM
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: 'highlight'

# Set the Sass partials directory, as we're using @imports
sass:
  style: :expanded

plugins:
  - jekyll-sitemap
  - jekyll-feed

# Exclude these files from your production _site
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - CNAME
  - cv-source
  - tran duc tri.jpg
